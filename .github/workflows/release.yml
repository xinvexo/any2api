name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: Release version, without the v prefix (for example, 0.1.1)
        required: true
        type: string

concurrency:
  group: release
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  linux-amd64:
    runs-on: ubuntu-22.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: pnpm/action-setup@v4
        with:
          version: 11.13.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - uses: dtolnay/rust-toolchain@1.90.0
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Resolve release tag
        env:
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          if [[ ! "$INPUT_VERSION" =~ ^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$ ]]; then
            echo "release version '$INPUT_VERSION' must be a stable SemVer without the v prefix" >&2
            exit 1
          fi
          release_tag="v$INPUT_VERSION"
          if git show-ref --verify --quiet "refs/tags/$release_tag"; then
            echo "release tag '$release_tag' already exists" >&2
            exit 1
          fi
          echo "RELEASE_TAG=$release_tag" >> "$GITHUB_ENV"
          echo "ANY2API_BUILD_VERSION=$INPUT_VERSION" >> "$GITHUB_ENV"

      - run: pnpm install --frozen-lockfile
      - name: Build distributable release artifacts
        run: pnpm package --target x86_64-unknown-linux-gnu

      - name: Publish
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh release create "$RELEASE_TAG" dist/* \
            --generate-notes \
            --target "$GITHUB_SHA" \
            --title "$RELEASE_TAG"
