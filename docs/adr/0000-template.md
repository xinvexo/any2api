# ADR-NNNN: 决策标题

- 状态：Proposed
- 日期：YYYY-MM-DD
- 决策者：maintainer
- 架构落点：`ARCHITECTURE.md` 的对应章节

仅当一个取舍无法合理并入 ADR-0170、且确实需要独立生命周期时才使用本模板。当前字段、接口、默认值、
状态机和测试矩阵只写入 `ARCHITECTURE.md` 或代码，不在 ADR 中复制。

## 背景

引用架构基线中的已有事实，说明为什么现在需要作出独立决定；不要重抄事实正文。

## 决策

记录方案方向、取舍和理由。精确边界与工程不变量同步写入架构基线，并在这里链接对应章节。

## 备选方案

列出认真考虑过的其他方案及未采用原因。

## 后果

记录正面影响、代价、迁移要求和后续验证方式。

## 验证

列出测试、CI 检查、运行指标或人工评审标准。
